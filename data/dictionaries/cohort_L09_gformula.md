# Cohort Dictionary: L09 G-formula/Standardization

**Cohort extract:** `cohort_L09_gformula.parquet` (or `.csv`)

## Description

Baseline/early exposure cohort for g-formula/standardization analysis. Same substantive cohort as L08 (can use same extract).

## Variables

See `cohort_L08_ps_ipw.md` for variable definitions.

## Notes

- Typically uses the same cohort extract as L08
- Focus on outcome model E[Y|A,L] rather than propensity model
